# Claude Code Setup

Use AgentBubble by keeping `.agentbubble/` at the project root.

`.agentbubble/` remains the source of truth.

Optional setup:

- create `CLAUDE.md` at the project root
- point Claude Code to `.agentbubble/session-start.md`
- optionally point to `.agentbubble/coding-rules.md`

Do not duplicate the entire framework into `CLAUDE.md`.

Use `CLAUDE.md` only as a lightweight pointer.
