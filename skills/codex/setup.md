# Codex Setup

Use AgentBubble from the project root.

`.agentbubble/` remains the source of truth.

Start Codex from the project root, then use `prompts/start-session.md` or paste the local session bootstrap.

Do not paste the full framework every session.
