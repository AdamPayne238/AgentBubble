# Architecture

This is a lightweight architecture map generated by `agentbubble init` from deterministic local file signals.

For any section that cannot be determined from the codebase, write:
[UNKNOWN — ask project owner]

Do not guess.
Do not infer.
Mark it and move on.

## Stack

Framework: None — vanilla Node.js CLI (ESM, "type": "module", Node >= 18)
Package manager: npm (no lockfile — no external dependencies)
TypeScript config: None (plain JavaScript)

## App Structure

bin/ — CLI entry point (agentbubble.js)
lib/ — core logic (init.js, audit.js, detect.js, constants.js, ticket.js, path-match.js)
templates/base/.agentbubble/ — files copied to target projects on `agentbubble init`
prompts/ — prompt template files for agents
skills/ — adapter-specific skill files (chatgpt/, claude/, codex/, cursor/, windsurf/)
test/ — Node.js built-in test files (audit.test.js, detect.test.js, init.test.js)

## Key Directories

bin/, lib/, templates/base/.agentbubble/, prompts/, skills/, test/

## Important Data Models

None — no database, no persistent data models. State is file-based (.agentbubble/ in the target project).

## Auth / Permissions

None

## Payments / Billing

None

## External Services

Database/tooling: None
Environment example: None (.env.example not present)

## Testing Commands

node --test

## Build Commands

None — pure JavaScript, published as-is to npm

## Deployment Notes

CI tooling: None
